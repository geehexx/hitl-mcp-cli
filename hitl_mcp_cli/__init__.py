"""HITL MCP CLI — Human-in-the-Loop MCP Server.

Public modules:
    - ``server``     — re-exports the FastMCP instance and tool functions
    - ``tools``      — tool primitives (model-controlled actions)
    - ``resources``  — resource primitives (read-only data: queue / session state)
    - ``prompts``    — prompt primitives (reusable HITL templates)
"""

__version__ = "1.0.0rc1"
